# mashjid-project-backend
# ⚙️ Installation Guide

## 🪄 Step 1: Install Node.js and npm

### 🔹 For Windows / macOS
1. Go to the official Node.js website:  
   👉 [https://nodejs.org](https://nodejs.org)
2. Download the **LTS (Long-Term Support)** version.
3. Run the installer and complete the setup.
4. After installation, verify it using:

```bash
node -v
npm -v
```
### After install node.js now clone this repo
```bash
git clone https://github.com/alamshuvo/mashjid-project-backend.git
cd mashjid-project-backend
npm install
```
### 🧾  Environment Variables

Create a `.env` file in the root directory by copying the provided `.env.example` file and update it with your credentials. This file is locked in `.env.example` for reference only.

```start your project
npm run dev
```



